import { QuillEditorsDirective } from './quill-editors.directive';

describe('QuillEditorsDirective', () => {
  it('should create an instance', () => {
    const directive = new QuillEditorsDirective();
    expect(directive).toBeTruthy();
  });
});
